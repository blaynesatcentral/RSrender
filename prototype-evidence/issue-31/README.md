# RSrender issue #31 prototype evidence

Throwaway primary-source artifacts for the document-ownership and commit-authority decision. Nothing here is application code.

- Open `ownership-state-model.html` directly for the guided logic walkthrough.
- From this worktree root, run `python prototype-evidence/issue-31/run_empirical.py` to create a fresh inert local-Windows result set.
- `findings.md` records the authoritative run, stable state/conflict vocabulary, rejected alternatives, and proposed ADR.

The captured run uses synthetic local files. It does not prove Electron, SMB/cross-machine, power-loss, Save As, or production-journal behavior.
